# SPDX-License-Identifier: CC0-1.0

task install_and_set_as_system_locale {
	if distro == fedora {
		dnf install "glibc-locale-source"
	}

	exec as root "./install" reverse {}

	exec "./set-as-system-locale" reverse {}
}
