# SPDX-License-Identifier: CC0-1.0

distro task install_package
	fedora {
		dnf install "tmux"
	}
	termux {
		pkg install "tmux"
	}
	ubuntu {
		apt install "tmux"
	}

task install_configuration {
	copy "tmux.conf" to "$XDG_CONFIG_HOME/tmux/tmux.conf"
}
