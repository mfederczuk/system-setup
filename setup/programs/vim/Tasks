# SPDX-License-Identifier: CC0-1.0

distro task install_package
	fedora {
		dnf install "vim-minimal"
	}
	termux {
		pkg install "vim"
	}
	ubuntu {
		apt install "vim"
	}

task install_configuration {
	copy "vimrc.vim" to "$HOME/.vimrc"
}
