# SPDX-License-Identifier: CC0-1.0

[desktop-environment(*) && distro(fedora-41)]
install-package-repositories (
	ExecShell as root "dnf copr enable pgdev/ghostty"
)

[desktop-environment(*) && distro(fedora-41)]
install {
	ExecShell as root "dnf install ghostty"
}

[desktop-environment(*)]
configure {
	...
}
